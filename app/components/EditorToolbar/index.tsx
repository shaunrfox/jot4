import {
  EditorProvider,
  FloatingMenu,
  BubbleMenu,
  // useEditorState,
} from "@tiptap/react";

const EditorToolbar = () => {
  return (
    <div
      style={{
        background: "#eee",
        padding: "1rem",
      }}
    >
      Toolbar
    </div>
  );
};

export default EditorToolbar;
